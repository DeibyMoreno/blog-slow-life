type Tag {
  id: UUID!
  name: String!
  slug: String!
  createdAt: DateTime!
}

input CreateTagInput {
  name: String!
}

extend type Query {
  tags: [Tag!]!
  tag(id: UUID): Tag
}

extend type Mutation {
  createTag(input: CreateTagInput!): Tag!
}
